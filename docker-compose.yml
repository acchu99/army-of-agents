version: "3.9"

services:
  agent-army:
    build: .
    stdin_open: true
    tty: true
    env_file:
      - .env
    volumes:
      - .:/app                       # mount everything for flexibility
      - ./agents:/app/agents         # edit prompts without rebuilding
      - ./config.yaml:/app/config.yaml